// -----------------------------------------------------------------------------\n// builtin-exec — execute shell commands\n// -----------------------------------------------------------------------------\n// NOTE: This is a STUB implementation for RED PHASE testing\n// The actual implementation will be added in Phase 4 of the builtin tool enhancements plan\n\nexport interface BuiltinExecArgs {\n  argv: string[];\n  timeout?: number;\n  workdir?: string;\n  stdin?: string;\n}\n\nexport interface BuiltinToolContext {\n  workspacePath: string;\n}\n\nexport const builtinExec = async (\n  args: BuiltinExecArgs,\n  ctx: BuiltinToolContext,\n): Promise<{ resultJson: string }> => {\n  // This is a stub implementation that will fail tests intentionally\n  // The real implementation will properly handle multiline strings\n  const { argv, timeout = 30000, workdir, stdin } = args;\n\n  // RED PHASE: Return incorrect behavior to make tests fail\n  return {\n    resultJson: JSON.stringify({\n      stdout: \"incorrect stub output\",\n      stderr: \"\",\n      exitCode: 0,\n    }),\n  };\n};