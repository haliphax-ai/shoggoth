# `@shoggoth/os-exec`

Subprocess execution with UID/GID drop, and `read` / `write` / `exec` tool helpers scoped to agent workspaces (DAC-aware).

- **Source:** `src/`
- **Tests:** `test/` (`npm run test`)
- **Typecheck:** `npm run typecheck` → `tsc --noEmit`
