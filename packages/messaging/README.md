# `@shoggoth/messaging`

Internal message model, agent-to-agent bus, outbound transport, Discord gateway/REST adapters, streaming handles, and capability descriptors.

- **Source:** `src/`
- **Tests:** `test/` (`npm run test`)
- **Typecheck:** `npm run typecheck` → `tsc --noEmit`
